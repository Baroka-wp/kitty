<!DOCTYPE html>
<html>
  <%= render 'shared/head' %>
  <body class="container">
    <%= render 'shared/topnav' %>
    <main>
      <p id="alert"><%= alert %></p>
      <p id="notice"><%= notice %></p>
        <div class="row">
          <div class="col-md-4 col-lg-3">
            <%= render "profils/share/sidebar"%>
          </div>
          <div class="col-md-8 col-lg-9">
            <%= yield %>
          </div>
        </div>
    </main>
    <%= render "shared/footer" %>
  </body>
</html>
